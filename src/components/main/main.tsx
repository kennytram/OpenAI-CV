import React, { useState, useEffect } from "react";
import { gapi } from "gapi-script";

interface MainProps {
  setIsLoading: React.Dispatch<React.SetStateAction<boolean>>; 
}

const Main: React.FC<MainProps> = ({ setIsLoading }) => {
  

  return (
    <div className="App">

    </div>
  );
};

export default Main;
